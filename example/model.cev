package main;

import "./import";

enum Gender {
    Male = "male";
    Female = "female";
}

enum Role {
    Admin;
    User;
}

record Person {
    name: string;
    age: int;
    role: Role
    gender: Gender;
}